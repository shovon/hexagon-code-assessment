# Ecommerce software

This may become the next Amazon.

## Getting started

On one terminal, run:

```bash
cd frontend
npm i
```

## Running

On one terminal, run:

```bash
cd frontend
npm start dev
```

And on another terminal, run:

```bash
cd backend
dotnet run
```
